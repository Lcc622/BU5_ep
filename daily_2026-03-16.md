# 2026-03-16 开发日报

## 今日修复与改动汇总

---

### 1. 跟卖处理器 BK28/30/02 行数据稀疏问题

**问题**：跟卖上新生成的 Excel 中，同颜色的后几个尺码（如 BK28、BK30、BK02）字段大量为空，缺少 `brand_name`、`feed_product_type` 等 Category 字段。

**根本原因**：
- `reference_row`（参考行）查找逻辑只在老款 SKU（无 `-` 后缀，如 `EG02084BK04`）中寻找有 Category 数据的行
- 但实际 Category 上传文件里存储的是新款格式的 SKU（如 `EG02084BK04-UK1`，含 `-UK1` 后缀）
- 这些新款 SKU 被 `scan_old_skus_by_prefix` 排除在外，导致 `reference_row` 永远是 `None`

**修复**：在 `follow_sell_processor.py` 的 `reference_row` 查找逻辑中新增回退：当老款 SKU 中找不到有 Category 数据的行时，进一步扫描同一颜色前缀（如 `EG02084BK`）下所有 `by_sku` 条目（包含 `-UK1` 后缀的新款），找到第一个有 `category_data` 的条目作为参考行，再通过 `{**reference_row, **old_row}` 将 brand_name 等字段合并进稀疏行。

---

### 2. 加色加码主图 URL 后缀错误

**问题**：加色加码导出表中 `main_image_url` 列生成的 URL 后缀是 `-L`（如 `EG02643BD-L.jpg`），但图片服务器上主图文件名格式为 `-L1`（如 `EG02643BD-L1.jpg`）。

**根本原因**：`config.py` 中所有国家的 `image_suffix_main="-L"` 配置错误，应为 `-L1`。同时，`image_suffixes_other` 列表的第一项也是 `-L1`，如果直接改主图后缀不同步处理会造成重复。

**修复**：
- 将 `config.py` 中 5 个国家的 `image_suffix_main` 从 `"-L"` 改为 `"-L1"`
- 同步从每个国家的 `image_suffixes_other` 列表中移除 `"-L1"`（由 5 项缩减为 4 项）
- 同步更新测试断言

**生效后**：
```
main_image_url → https://eppic.s3.amazonaws.com/EG02643BD-L1.jpg ✓
Other Image Url1 → https://eppic.s3.amazonaws.com/EG02643BD-L5.jpg
Other Image Url2 → https://eppic.s3.amazonaws.com/EG02643BD-L3.jpg
Other Image Url3 → https://eppic.s3.amazonaws.com/EG02643BD-L4.jpg
Other Image Url4 → https://eppic.s3.amazonaws.com/EG02643BD-S-UK.jpg
Other Image Url5 → （留空）
```

---

### 3. Package Width 字段写入模板失败

**问题**：加色加码导出表中 `package-width`（包装宽度）列始终为空。

**根本原因**：`COPY_FIELD_ALIASES` 中 `package_width` 的 output_key 是 `"Package Width"`，而模板的 display header 是 `"package-width"`、machine header 是 `"package_width"`。`_resolve_template_col_idx` normalize 后 `"package width"` 无法匹配 `"package-width"` 或 `"package_width"`（空格 vs 连字符/下划线），导致列索引返回 `None`，数据永远写不进去。

**修复**：将 `add_color_size_processor.py` 的 `COPY_FIELD_ALIASES` 中该条目的 output_key 从 `"Package Width"` 改为 `"package_width"`，与模板的 machine header 完全一致。

---
