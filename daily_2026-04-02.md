# 日报 2026-04-02

## 今天总体进展

修复 FR/IT 加色输出的多个字段问题（category sheet 选错、ASIN 取错、模板/字段值不匹配），并新增「直接贴 SKU」输入模式，用户可跳过前缀×颜色×尺码的矩阵选择，直接粘贴目标 SKU 列表。

---

## 第一块：FR/IT Category Sheet 选错 Bug（根因）

### 问题

FR 加色输出 `feed_product_type` 列全部为空、所有 category 字段缺失。实际 category_data 为空字典。

### 根因

`ListingIndexer._find_category_sheet()` 只识别英文 sheet 名 `"Template"`，但 FR 文件用 `"Modèle"`、IT 用 `"Modello"`。导致误选了 `"Définitions des données"`（数据定义 sheet），该 sheet 的行内容含 `"contribution_sku#1.value"` 字符串，被误判为表头行。

### 修法（`backend/app/core/indexers/listing_indexer.py`）

```python
# 新增本地化 sheet 名
_TEMPLATE_SHEET_NAMES = frozenset({"template", "modèle", "vorlage", "modello", "plantilla"})

# _find_category_sheet() 中优先匹配这些名字
template_name = next(
    (name for name in ordered_names if name.casefold() in self._TEMPLATE_SHEET_NAMES),
    None,
)
```

### 各国影响

| 国家 | Sheet 名 | 修复前 | 修复后 |
|------|---------|--------|--------|
| UK | Template | ✅ 正常 | 不受影响 |
| FR | Modèle | ❌ 误选 Définitions des données | ✅ 正确选择 Modèle |
| DE | Vorlage | ✅ 正常（无干扰 sheet） | 不受影响 |
| IT | Modello | ❌ 误选 Definizioni dati | ✅ 正确选择 Modello |
| ES | Plantilla | ⏳ 待验证 | 预期正常 |

---

## 第二块：Parent 行 ASIN 取错

### 问题

Parent 行的 `external_product_id` 取到了子体 ASIN 而非父体 ASIN（如 EE00736 输出了子 ASIN `B0CQN8VF2H`，正确的父 ASIN 应为 `B0CW2XP14S`）。

### 根因

`_build_parent_row` 使用 `source_record.asin`，而 `source_record` 是从子 SKU 记录中选出的（由 `_pick_source_record` 返回），其 ASIN 是子体的。

### 修法（`backend/app/core/processors/add_color_size_processor.py`）

新增 `_lookup_parent_asin()` 方法，从 index 的 `by_sku` 中查找纯产品码（如 `EE00736`）对应的父 SKU 记录的 ASIN：

```python
def _lookup_parent_asin(self, index, parent_sku):
    by_sku = index.get("by_sku", {})
    raw = by_sku.get(parent_sku)
    if raw is not None:
        record = self._normalize_record(raw)
        if record is not None and record.asin:
            return record.asin
    return None
```

处理循环中在 `_build_parent_row` 之前调用，优先使用父 ASIN，fallback 到 source_record.asin。

---

## 第三块：ASIN 只在 Parent 行

### 问题

`external_product_id` + `external_product_id_type` 出现在所有子行上，应只在父行。Amazon 自动生成子 ASIN。

### 修法

- 从 `_build_output_row`（子行）中移除 ASIN 赋值逻辑
- 在 `_build_parent_row`（父行）中新增 ASIN 赋值

---

## 第四块：FR 模板与字段值更新

### 问题清单

| 字段 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| FR 模板文件 | FR补色模板.xlsm (2020) | ES02725.xlsx (2023) | 新模板含 GPSR 字段 |
| feed_product_type | DRESS | dress | 新模板要求小写 |
| apparel_size_system | FR | FR / ES | FR/ES 共用 |
| list_price_with_tax | standard_price + 10 | standard_price + 5 | 规则修正 |
| list_price | 从源数据复制 | 排除不填 | 只填 list_price_with_tax |
| manufacturer | 无 | Dongguan Kaidilisha Fushi Co., Ltd | 新增固定字段 |
| dsa_responsible_party_address | 无 | Info@apex-ce.de | 新增固定字段 |
| gpsr_manufacturer_reference | 无 | globalservice@ever-pretty.com | 新增固定字段 |

### 修法要点

- `_build_country_static_fields(FR)` 新增 manufacturer / dsa / gpsr 三个固定字段
- `_build_output_row` 中 Product Type 赋值后做 `.lower()`
- `list_price` / `list price` 加入 `LOCKED_SOURCE_FIELD_ALIASES` 防止源数据覆盖

---

## 第五块：直接贴 SKU 输入模式（新功能）

### 背景

原流程：选 prefix → 选 colors → 选 size 区间 → 笛卡尔积生成所有组合。用户痛点：每个款需要的颜色/尺码不同，只能逐个款逐个色操作，太繁琐。

### 新模式

用户直接粘贴目标 SKU（如 `EE00685BK04, EE00685BK06, EE01960DR04`），系统自动解析、分组、生成。

### 后端改动

**`backend/app/models/excel.py`**
- `ProcessRequest` 新增 `input_mode: Literal["matrix", "direct-sku"]` 和 `direct_skus: list[str] | None`
- 原有 selected_prefixes / target_colors / start_size / end_size 改为可选

**`backend/app/core/processors/add_color_size_processor.py`**
- 新增 `_build_target_variants(direct_skus)` — 解析 SKU 列表，按 product_code 分组得到 `{prefix: [(color, size), ...]}`
- `_process_core` 中：`direct-sku` 模式用 target_variants 替代笛卡尔积，`matrix` 模式保持不变
- `_resolve_output_filename` 适配 direct-sku 模式

**`backend/app/api/excel.py`**
- `_validate_process_files` 根据 input_mode 做不同校验

### 前端改动

- **InputModeSwitcher.tsx**（新组件）：tab 切换「前缀×颜色×尺码」/「直接贴 SKU」
- **DirectSkuInput.tsx**（新组件）：大文本框 + 解析预览（SKU 数 / 产品码数 / 颜色数）
- **directSku.ts**（新工具）：前端 SKU 解析逻辑，支持换行/逗号/空格分隔
- **ProcessButton.tsx**：按 inputMode 分流校验和请求构造
- **App.tsx**：direct-sku 模式下隐藏 PrefixInput + ColorSelector，显示 DirectSkuInput

---

## 第六块：FR 其他字段修复

### 问题清单

| 字段 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| list_price | 从源数据复制（67.99） | 排除不填 | 源数据 list_price 被误复制到模板 list_price 列 |
| recommended_browse_nodes | 无 | 13880102031 | FR 固定节点值 |
| package_weight_unit_of_measure | KG | GR | FR 重量单位应为克 |
| Other Image Url5 (-S-EU) | ES0128BWH-S-EU.jpg | ES0128B-S-EU.jpg | EU 尺码表不含颜色码 |

### 修法要点

- `list_price` / `list price` 加入 `LOCKED_SOURCE_FIELD_ALIASES`，阻止源数据覆盖
- `_build_country_static_fields(FR)` 新增 `recommended_browse_nodes: "13880102031"`，weight 单位改 `GR`
- 图片循环中，`-S-EU` 后缀特判：URL 格式为 `{product_code}-S-EU.jpg`（不含颜色码）；UK 的 `-S-UK` 仍保留颜色码

---

## 今日 Commit 列表

| Commit | 说明 |
|--------|------|
| `219c5e8` | fix: FR/IT category sheet detection & parent-only ASIN output |
| `8d1396c` | fix: use parent SKU's own ASIN instead of child's ASIN for parent row |
| `0329bdf` | feat: add direct-SKU input mode for add-color/add-code processing |
| `1429a5b` | fix: update FR template and fix field values (dress/size/price/GPSR) |
| `466f2ff` | fix: exclude list_price from source data copy to prevent wrong column fill |
| `7a66947` | fix(FR): browse nodes, weight unit, EU size chart image URL |

---

## 待办

- ES 加色：需补充 ES 的 COPY_MARKETPLACE_LANGUAGE_ALIASES 和模板
- DE/IT 模板更新：可能也需要替换为新版模板（含 GPSR 字段）
- 直接贴 SKU 模式前端联调测试
