# 2026-03-17 开发日报

## 今日修复汇总

---

### 1. 跟卖 - BK28/30/02 行数据稀疏问题

**问题**：跟卖生成的 Excel 里，同颜色后几个尺码（BK28、BK30、BK02）的 brand_name、feed_product_type 等字段全是空的。

**原因**：代码里有个"参考行"逻辑，会找同色组里有 Category 数据的行，用来给没数据的行补字段。但是这个查找只在老款 SKU（无后缀，如 EG02084BK04）里找，而实际 Category 文件里存的都是带 `-UK1` 后缀的新款 SKU。结果参考行永远找不到，BK28/30/02 就一直是空的。

**修复**：加了一个回退逻辑——如果老款 SKU 里找不到参考行，就去 `by_sku` 里扫描同一颜色前缀下所有条目（包括带 `-UK1` 后缀的），找到第一个有 Category 数据的就拿来用。

---

### 2. 加色加码 - 主图 URL 后缀错误

**问题**：加色加码生成的表里，`main_image_url` 是 `EG02643BD-L.jpg`，但图片服务器上主图实际是 `-L1` 结尾的。

**原因**：`config.py` 里 5 个国家的 `image_suffix_main` 都配的是 `-L`，配错了。而且 `image_suffixes_other` 里第一个也是 `-L1`，如果直接改主图不动附图就会重复。

**修复**：把 5 个国家的 `image_suffix_main` 从 `-L` 改成 `-L1`，同时把 `image_suffixes_other` 里的 `-L1` 移除，每个国家从 5 个附图缩为 4 个。同步更新了测试断言。

---

### 3. 加色加码 - package-width 字段写不进模板

**问题**：加色加码导出表里 `package-width`（包装宽度）列始终是空的。

**原因**：代码里这个字段的输出 key 是 `"Package Width"`（中间有空格），但模板的列名是 `"package-width"`（连字符）和 `"package_width"`（下划线）。normalize 之后 `"package width"` 怎么也匹配不上，列找不到，数据自然写不进去。

**修复**：把 `COPY_FIELD_ALIASES` 里这条的 output key 从 `"Package Width"` 改成 `"package_width"`，直接对应模板的 machine header，能精确匹配了。

---

### 4. 跟卖 - `-UK1+` 后缀未归一化

**问题**：SOP 里说 `-UK1+` 后缀要统一处理成 `-UK1`，但代码直接把带 `+` 的 SKU 当成无效 SKU 丢掉了。

**原因**：`parse_sku` 的正则末尾有 `$`，`-UK1+` 里的 `+` 匹配失败，直接抛异常，SKU 进了 `invalid_skus` 列表。

**修复**：在 `follow_sell_processor.py` 里，调用 `parse_sku` 之前先 `rstrip("+")` 清掉末尾的加号，再解析。加了对应测试用例。

---

## 测试结果

全量测试：**31 passed**

## 遗留问题

- `sleeve_type` 字段为空：这是数据问题，不是代码 bug。Category Listings 文件里这款产品没填 Sleeve Type，需要在源文件里补全后重新上传。
